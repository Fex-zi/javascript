# javascript

Jvascript : PHP
.split : explode() //convert text into an array
.join : implode() // to join/add to arrays( , -)
.slice : array_slice() // to remove arrays(1, -2)
mvs.filter(function (move) {
return move > 0;
}); : array_filter($mvs, function($move) {
return $move > 0;
});
