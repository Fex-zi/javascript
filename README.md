# javascript

/////------ARRAY METHODS----///
Jvascript : PHP
.split : explode() //convert string into an array/ can't for int
.join : implode() //convert array to strings and works with intjoin/adds( , -)
.slice : array_slice() // to remove arrays(1, -2)
.reduce : reduce() or array_reduce// to sum up the array
.filter : array_filter(); // to filter array
.map: array_map() // works like foreach but returns in array format without mutating/changing original array
.flat(): array_merge()//joins different or nested arraystogether
.flatMap(): ?? // joins .map & .flat
.sort() : sort() or a/ar/k/krsort() // not good since it mutates/changes array

///-count--//
count = 5
++count = 6 : count++ =5
